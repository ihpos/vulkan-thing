module(name = "third_party")

bazel_dep(name = "platforms", version = "1.0.0")

use_repo(use_extension(":repositories.bzl", "deps"), "glfw", "glm", "vulkan")
use_repo(use_extension("@third_party//vulkan:repositories.bzl", "deps"), "vulkan_headers_archive", "volk")